<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css" type="text/css" /> 
  <link rel="stylesheet" href="styles.css" type="text/css" />
  <title>Elections Near You</title>
</head>
<body>
  
  <form action ="/" method="POST">
    <div class = "container">
      <div class = "row">
        <div class = "six columns">
          <h2>Elections near you</h1>
          <label for ="street">Street Address:</label>
          <input type ="text" id ="street" name = "street" class="u-full-width"/>
        </div>
      </div>
      
      <div class = "row">
        <div class = "six columns">
          <label for= "address">City:</label>
          <input type ="text" id = "city" name = "city" class="u-full-width"/>  
        </div>
      </div>
      
      <div class = "row">
        <div class = "six columns">
          <label for= "state">State:</label>
          <select id ="state" name ="state">
            <% for(let postalAbb of states){%>
              <option value = <%= postalAbb %>>  <%= postalAbb %></option>
            <% } %>
          </select>
        </div>
      </div>
      
      <div class = "row">
        <div class = "six columns">
          <label fo = "zipcode">Zipcode:</label>
          <input type = "text" id = "zipcode" name ="zipcode"class="u-full-width"/>
        </div>
      </div>
      <input type= "submit" class="button-primary" />
    </div>
  </form>
  
  
  <section id = "elections">
    <% if (electionData) { %>
      <% for(let info of electionData) { %>
        <h3>Date: <%= info.date %></h1>
        <h3>Type of Election: <%= info.type %></h2>
        <h3>Description: <%= info.description %></h2>
        <a href="<%= info.url %>">Url for voters search information</a>
      <% } %>
    <% } %> 
    
    <% if(electionData === []) { %>
      <p><%= "If the page refresh after you hit submit and it did not show that means there was no results." %> </p>
    <% } %>

    
    
  </section>
  
</body>
</html>